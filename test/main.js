var fixturePath    = 'base/test/spec/fixtures',
    regularFixture = 'regular_carousel.fixture.html';
    singleFixture  = 'single_image.fixture.html';

function imgName(src) {
	return src.split('/').pop();
}